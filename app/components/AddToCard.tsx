"use client";
import React from "react";

function AddToCard() {
  return (
    <div>
      <button onClick={() => console.log("Click")}>Add to Cart</button>
    </div>
  );
}

export default AddToCard;
